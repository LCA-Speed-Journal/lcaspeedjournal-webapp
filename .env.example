# Copy to .env.local and fill in values. Do NOT commit .env.local.

# From Vercel: project → Storage → Postgres → Connect
# REQUIRED for createPool(): the URL must contain the literal substring "-pooler."
# (e.g. host: xxx-pooler.region.vercel-storage.com). If your URL has no "-pooler.",
# use the other connection string from the dashboard (pooled vs direct).
POSTGRES_URL="postgres://..."

# Run: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-here"

# Local: http://localhost:3000  |  Production: https://your-app.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# Coach PIN for MVP (default: 1234)
COACH_PIN="1234"
